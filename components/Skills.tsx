import SkillsHeader from "./SkillsHeader";
import SkillsTabs from "./SkillsTabs";

const Skills = () => {
  return (
    <section className="max-container my-20 min-h-screen" id="skills">
      <SkillsHeader />
      <SkillsTabs />
    </section>
  );
};

export default Skills;
