import SkillsHeader from "./SkillsHeader";
import SkillsTabs from "./SkillsTabs";

const Skills = () => {
  return (
    <section className="max-container py-20 min-h-screen">
      <SkillsHeader />
      <SkillsTabs />
    </section>
  );
};

export default Skills;
